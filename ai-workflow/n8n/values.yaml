# =============================================================================
# n8n - Helm (8gears) Values (Demo)
# =============================================================================

main:
  replicas: 1
  config:
    DB_TYPE: postgresdb
    DB_POSTGRESDB_HOST: postgres-grafana-postgresql.postgres-system.svc
    DB_POSTGRESDB_PORT: "5432"
    DB_POSTGRESDB_USER: admin
    DB_POSTGRESDB_DATABASE: n8n_db
    WEBHOOK_URL: "https://n8n.dev.dawet.internal"
    N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS: "false"
  secret:
    DB_POSTGRESDB_PASSWORD: admin-db-123
  
  persistence:
    enabled: true
    storageClass: hostpath
    size: 2Gi

service:
  type: NodePort
  port: 5678
  nodePort: 30520

# Use labels that match the fleet bundle if needed
labels:
  bundle: n8n
