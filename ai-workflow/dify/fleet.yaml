# =============================================================================
# Dify - Fleet Bundle (Demo)
# =============================================================================

defaultNamespace: ai-workflow

helm:
  repo: https://difyai.github.io/helm-charts
  chart: dify
  version: 0.3.0
  releaseName: dify
  valuesFiles:
    - values.yaml

dependsOn:
  - selector:
      matchLabels:
        bundle: minio
  - selector:
      matchLabels:
        bundle: redis
  - selector:
      matchLabels:
        bundle: postgres-cluster
  - selector:
      matchLabels:
        bundle: milvus

targetCustomizations:
  - name: dev
    clusterSelector:
      matchLabels:
        env: dev

labels:
  bundle: dify
