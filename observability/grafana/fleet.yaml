#  =============================================================================
# Grafana - Fleet Bundle (Demo)
#  =============================================================================
# DISABLED — Grafana is now bundled inside kube-prometheus-stack
# which provides default Kubernetes dashboards out of the box.
# See: observability/kube-prometheus-stack/values.yaml -> grafana.enabled: true
#  =============================================================================

# defaultNamespace: observability
#
# helm:
#  repo: https://grafana.github.io/helm-charts/
#  chart: grafana
#  version: 10.5.15
#  releaseName: grafana
#  valuesFiles:
#    - values.yaml
#
# targetCustomizations:
#  - name: dev
#    clusterSelector:
#      matchLabels:
#        env: dev
#
# labels:
#  bundle: grafana
