# =============================================================================
# Grafana - Fleet Bundle (Demo)
# =============================================================================

defaultNamespace: observability

helm:
  repo: https://grafana.github.io/helm-charts/
  chart: grafana
  version: 8.4.0
  releaseName: grafana
  valuesFiles:
    - values.yaml

dependsOn:
  - selector:
      matchLabels:
        bundle: postgres-cluster
  - selector:
      matchLabels:
        bundle: mimir
  - selector:
      matchLabels:
        bundle: loki
  - selector:
      matchLabels:
        bundle: tempo

targetCustomizations:
  - name: dev
    clusterSelector:
      matchLabels:
        env: dev

labels:
  bundle: grafana
