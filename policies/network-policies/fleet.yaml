#  =============================================================================
# Network Policies - Fleet Bundle (Demo)
#  =============================================================================
# Step 13
#  =============================================================================
defaultNamespace: ""

dependsOn:
 - selector:
     matchLabels:
       bundle: grafana

targetCustomizations:
 - name: dev
   clusterSelector:
     matchLabels:
       env: dev

labels:
 bundle: network-policies
